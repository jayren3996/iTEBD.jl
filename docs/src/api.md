# API Reference

This page is generated directly from the source-code docstrings.

Use it when you want the exact function signatures, argument definitions,
keyword defaults, return conventions, or short API-level examples. For a more
narrative explanation of the workflows and tensor conventions, start with the
manual pages such as [iMPS and Canonical Form](imps.md),
[Time Evolution](time-evolution.md), and [ScarFinder](scarfinder.md).

```@autodocs
Modules = [iTEBD]
Order = [:type, :function]
```
